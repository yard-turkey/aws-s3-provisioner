package reconciler

import (
	"context"

	"k8s.io/apimachinery/pkg/runtime"

	"sigs.k8s.io/controller-runtime/pkg/client"
)

// internalClient wraps related reconciler fields for convenient passing to helpers. Client is a controller-runtime
// DelegatingClient generated by the manager in manager.go. This is for optimization of API calls where in we make API
// resource requests against the store and writes against the API server.
type internalClient struct {
	client.Client
	ctx    context.Context
	scheme *runtime.Scheme
}
